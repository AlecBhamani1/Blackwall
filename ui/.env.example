# Browser-development proxy target. Use the Ollama/OpenAI-compatible endpoint on the model host.
# BLACKWALL_DEV_MODEL_ENDPOINT=http://your-model-host:11434

# Optional model to preselect after discovery.
# VITE_BLACKWALL_MODEL=your-model:latest
